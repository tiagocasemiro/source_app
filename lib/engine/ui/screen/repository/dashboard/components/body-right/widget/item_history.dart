import 'package:flutter/material.dart';

class ItemHistory extends StatelessWidget {
  final Function _onClick;

  ItemHistory(this._onClick(String hash));

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
